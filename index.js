/**
 * @format
 */

import { AppRegistry } from "react-native";
import Aplication from "./src/pages/authPage";
import { name as appName } from "./app.json";

AppRegistry.registerComponent(appName, () => Aplication);
